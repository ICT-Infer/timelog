--- a/serve/urls.py
+++ b/serve/urls.py
@@ -7,4 +7,5 @@
     # url(r'^blog/', include('blog.urls')),
 
     url(r'^admin/', include(admin.site.urls)),
+    url(r'^timelog/', include('timelog.urls')),
 ]
